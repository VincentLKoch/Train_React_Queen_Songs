import React from "react";

/* Not that usefull since header is a bit empty but it is when we start adding router */
export default function Header() {
  return <div className="header">Queens playlist selector</div>;
}
